language: python
python: 3.8

jobs:
  include:
    - python: '3.8'
    - python: '3.7'
    - python: '3.6'

install:
  - pip install -r requirements.txt
  - pip install -r dev-requirements.txt

before_script:
  - cd pur_beurre/

script:
  - python -m manage.py makemigrations
  - python -m manage.py migrate
  - python -m manage.py format_csv
  - python -m manage.py import_csv
  - coverage run -m manage test

after_success:
  - coveralls
